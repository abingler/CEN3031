describe('Protractor Demo App', function() {
  it('4', function() {
    browser.get('http://localhost:3000/');
    element(by.css('[data-ng-click="main.select(1)"]')).click();
    element(by.model('tech.problem')).sendKeys('freezes');
    element(by.css('[data-ng-show="tech.shouldShowSubmit()"]')).click();
    
    //var link = element(by.css('[data-ng-show="tech.shouldShowSuggest"]'));
    console.log('\n \n');
    console.log('Andre');
    //expect(link.getText()).toEqual('Rebooting your device may help. To reboot it, turn it completely off and back on again. ');

    element(by.css('[data-ng-click="tech.foundSolution()"]')).click();
  });
});


describe('Protractor Demo App', function() {
  it('4', function() {
    browser.get('http://localhost:3000/');
    element(by.css('[data-ng-click="main.select(1)"]')).click();
    element(by.model('tech.problem')).sendKeys('lost level');
    element(by.css('[data-ng-show="tech.shouldShowSubmit()"]')).click();
    
    //var link = element(by.css('[data-ng-repeat="curSuggest in tech.suggest"]'));
    //expect(link.getText()).toEqual('Make sure you are currently signed in to your Apple ID on your device. Check other apps where you have made purchases or go into the App Store on your device and make see if you are prompted to log in. ');

    element(by.css('[data-ng-click="tech.foundSolution()"]')).click();
  });
});