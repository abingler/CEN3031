describe('functionality testing suit', function(){
	it('should Identify the address', function(){
		browser.get('http://localhost:3000/');
		expect(browser.getCurrentUrl()).toContain('localhost:3000');
	})
})
		